# Optional token required when serving on a non-loopback interface.
COMMERCE_EVAL_API_TOKEN=

# Optional judge credentials belong to the provider adapter, never the database.
COMMERCE_EVAL_JUDGE_MODEL=

